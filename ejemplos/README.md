# Ejemplos

> 🚧 Borrador — falta completar.

Artefactos reutilizables y copiables: código y configuración primero, explicación mínima al lado. A diferencia de `docs/` (conceptual) y `casos-de-uso/` (narrativo), acá el foco es "copiá esto y adaptalo".

Subcarpetas previstas (se crean cuando haya contenido real):

- `claude-md/` — ejemplos de `CLAUDE.md` para distintos tipos de proyecto.
- `hooks/` — configuraciones de `hooks` en `settings.json` con casos de uso concretos.
- `skills/` — `skills` de ejemplo, con su estructura de archivos.
- `slash-commands/` — comandos `/` custom listos para copiar.
- `mcp/` — configuraciones de servidores MCP.
